\section{常数优化}

算法复杂度正确却被卡常（TLE）时，按以下优先级依次尝试。

\subsection{编译器优化开关}

\textbf{用途：} 在代码开头加入 \code{\#pragma} 优化指令，或使用更强的编译选项，让编译器自动完成循环展开、向量化等优化，是最简单有效的提效手段。

\lstinputlisting[language=C++]{codes/10-optimization-01.cpp}

\begin{quote}
  \textbf{注意：} 部分 OJ 会忽略 \code{\#pragma}（或要求指定编译选项），此时应改用 \code{-O2} / \code{-Ofast} / \code{-march=native} 编译；本地调试时建议不加优化指令，以免影响断点与变量查看。
\end{quote}
